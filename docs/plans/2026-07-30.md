# 📌 Jalon 0 — Assainissement, Alignement Décimal et Passage au Vert de la CI/CD

L'objectif de ce jalon préliminaire est d'éliminer les 3 erreurs Ruff, de corriger le crash unitaire sur `OrderReceipt`, et de purger la plomberie d'infrastructure obsolète afin de restaurer immédiatement un environnement au vert (Linting valide et Couverture stricte à 100,00 %).

### Sous-Jalon 0.1 : Nettoyage Géométrique (Ruff) & Rectification de Script
*   **Objectif** : Éliminer les erreurs de longueur de ligne et corriger le script d'analyse.
*   **Pré-requis** : Codebase actuelle en l'état.
*   **Sous-sous-étapes** :
    1. Ouvrir `strategies/mean_reversion.py` : scinder ou formater les lignes 48 et 49 sous la barre des 90 caractères (parenthèses pour les longs calculs ou retour à la ligne dans la docstring).
    2. Ouvrir `strategies/regime_manager.py` : couper la ligne 65 pour la faire passer sous les 90 caractères.
    3. Ouvrir `run_lint.sh` : supprimer la seconde occurrence du dossier `strategies/` dans la commande d'analyse de Ruff.
    4. Ouvrir `core/exceptions.py` : supprimer l'espace initial en colonne 1 sur les séparateurs graphiques `# ===...`.
    5. Ouvrir `tests/core/test_caching.py` et `tests/core/test_models.py` : ajouter la ligne de clôture réglementaire `# ===...` calée en colonne 1 en fin de fichier.
*   **Livrables Attendus** : L'exécution de `./run_lint.sh` se termine avec un succès de 100 %, zéro erreur trouvée.

### Sous-Jalon 0.2 : Alignement des Attributs du Dummy Adapter (Pytest)
*   **Objectif** : Aligner la fixture de test `DummyBrokerAdapter` sur le modèle décimal immuable d'`OrderReceipt` et d'`AccountSnapshot`.
*   **Pré-requis** : Sous-Jalon 0.1 validé.
*   **Sous-sous-étapes** :
    1. Ouvrir `tests/brokers/test_base_broker_interface.py`.
    2. Modifier la méthode `get_portfolio_snapshot` de la classe `DummyBrokerAdapter` pour qu'elle renvoie un `AccountSnapshot` (et non `PortfolioSnapshot`) avec les types décimaux : `balance=Decimal("100000.00")`, `equity=Decimal("102500.00")`, `available_margin=Decimal("95000.00")`.
    3. Modifier la méthode `place_order` de cette même classe pour qu'elle retourne un `OrderReceipt` valide : remplacer `broker_reference` par `broker_order_id`, ajouter le champ `status=OrderStatus.FILLED` et `average_execution_price=Decimal("1.0850")`, et supprimer l'argument de temps.
    4. Mettre à jour les assertions de la fonction `test_abstract_broker_bridge_contract_enforcement` pour interroger ces nouveaux champs décimaux exacts.
*   **Livrables Attendus** : Exécution locale de `pytest tests/brokers/test_base_broker_interface.py`. Succès unitaire.

### Sous-Jalon 0.3 : Purge des Fichiers Morts pour Restauration du Coverage
*   **Objectif** : Éteindre les alertes de sous-couverture (actuellement à 83,92 %) en éliminant les fichiers de l'ancienne infrastructure à état laissés en friche.
*   **Pré-requis** : Sous-Jalon 0.2 validé.
*   **Sous-sous-étapes** :
    1. Vider l'intégralité du contenu de `brokers/backtrader_strategy_bridge.py`.
    2. Vider l'intégralité du contenu de `brokers/simulated_adapter.py`.
    3. Vider l'intégralité du contenu de `strategies/mean_reversion.py`.
*   **Livrables Attendus** : Lancement combiné de `./run_lint.sh` puis de `./run_tests.sh`. Succès total, 100,00 % de couverture de code atteinte, la CI/CD est au vert.

## 📌 Jalon 1 — Réalignement Temporel du Modèle de Domaine

L'objectif est d'harmoniser la boussole temporelle du framework en migrant le timestamp de l'objet Order vers un type datetime conscient.

### Sous-Jalon 1.1 : Migration Typée de la Dataclass `Order`
*   **Objectif** : Remplacer l'entier Unix en millisecondes par un objet `datetime` pour conserver la gestion des fuseaux horaires.
*   **Pré-requis** : Jalon 0 finalisé au vert.
*   **Sous-sous-étapes** :
    1. Ouvrir `core/models.py` : modifier `Order.timestamp` de `int` à `datetime`.
    2. Ouvrir `tests/broker_adapters/test_base_broker_adapter.py` : remplacer l'entier `1719734400000` par une instance consciente `datetime(2026, 7, 30, 12, 0, tzinfo=ZoneInfo("UTC"))`.
    3. Ouvrir `tests/core/test_models.py` : appliquer la même correction au sein du test `test_order_types_with_limit_bounds`.
*   **Livrables Attendus** : Validation de la suite par `./run_tests.sh`. Succès unitaire complet.

---

## 📌 Jalon 2 — Le Moteur Synchrone Unique (`AegisExecutionEngine`)

Conception et assemblage du grand chef d'orchestre sans état capable de gérer les flux de données et de piloter les buffers de mémoire glissante.

### Sous-Jalon 2.1 : Matrice de Validation TDD de l'Engine (Phase RED)
*   **Objectif** : Rédiger le contrat de comportement et de routage de l'Engine sous forme de tests avant d'écrire son code.
*   **Pré-requis** : Jalon 1 au vert.
*   **Sous-sous-étapes** :
    1. Créer le fichier `tests/core/test_engine.py`.
    2. Implémenter un `MockMarketFeed` (itérateur conforme à `BaseMarketFeed`) délivrant une séquence finie de deux structures `MarketContext`.
    3. Écrire le scénario `test_engine_orchestrates_nominal_flow` : configure l'Engine avec un robot et un adaptateur factices, exécute la boucle et vérifie qu'un signal d'alpha a bien déclenché la soumission d'un `Order` décimal au courtier.
*   **Livrables Attendus** : Lancement de `pytest tests/core/test_engine.py` se soldant par un échec immédiat pour cause d'importation impossible. Phase RED validée.

### Sous-Jalon 2.2 : Écriture de la Boucle Itérative du Cœur de l'Engine
*   **Objectif** : Implémenter la structure de synchronisation et la gestion des buffers de cache glissant.
*   **Pré-requis** : Sous-Jalon 2.1 complété.
*   **Sous-sous-étapes** :
    1. Créer le fichier `core/engine.py` contenant la classe `AegisExecutionEngine`.
    2. Injecter au constructeur : l'instance de `BaseBot`, l'instance de `BaseBrokerAdapter`, et un dictionnaire fixant la capacité maximale de mémoire glissante par symbole.
    3. Implémenter la méthode `run_backtest(self, market_feed: BaseMarketFeed)`. La boucle doit tourner sur un `while True` consommant `next(market_feed)`.
    4. À chaque tick, extraire le prix de clôture, le pousser dans le `HistoricalBuffer` interne de l'actif, convertir la double file en liste plate via `.to_list()` et soumettre le tout à la méthode sans état `bot.evaluate()`.
*   **Livrables Attendus** : Exécution de `pytest tests/core/test_engine.py`. Succès unitaire total. Phase GREEN validée.

### Sous-Jalon 2.3 : Intégration du Sizer et du Risk Manager Décimaux
*   **Objectif** : Brancher les filtres de risques et de dimensionnement décimaux au cœur de la boucle de l'Engine.
*   **Pré-requis** : Sous-Jalon 2.2 validé.
*   **Sous-sous-étapes** :
    1. Modifier `AegisExecutionEngine` pour accepter par injection vos briques `PositionSizer` et `RiskManager`.
    2. Dans la boucle d'exécution, si l'intention `ExposureIntent` renvoie un `alpha_direction` non nul, appeler `PositionSizer.compute_volume()`.
    3. Acheminer le verdict au `RiskManager.assess_order_risk()` en extrayant l'`AccountSnapshot` décimal en temps réel depuis le courtier.
    4. Si le feu est vert, instancier l'objet immuable `Order` et appeler `BaseBrokerAdapter.submit_order(order)`.
*   **Livrables Attendus** : Exécution complète de `./run_tests.sh`. Succès global avec couverture maintenue à 100,00 % sur l'ensemble du package `core/`.

## 📌 Jalon 3 — L'Adaptateur Inbound Historique CSV (`LocalCsvDataLink`)

Transformation de l'ancien chargeur de fichiers plats en un itérateur de flux multi-actifs performant, conforme à l'interface BaseMarketFeed.

### Sous-Jalon 3.1 : Réécriture Fonctionnelle de la Passerelle Inbound
*   **Objectif** : Aligner le composant de données pour qu'il distribue ses lignes sous forme de structures `MarketContext`.
*   **Pré-requis** : Jalon 2 validé.
*   **Sous-sous-étapes** :
    1. Ouvrir `core/datalink.py` et faire hériter `LocalCsvDataLink` de `BaseMarketFeed`.
    2. Modifier sa méthode `load_asset_history` pour stocker le DataFrame Pandas localisé en UTC et initialiser un index de ligne courant (`self._current_index = 0`).
    3. Implémenter la méthode réglementaire `__next__(self) -> MarketContext`. Extraire la ligne du DataFrame correspondant à l'index, l'empaqueter dans un objet `MarketPricePoint` immuable, puis lever l'exception standard `StopIteration` dès que la fin du fichier est atteinte.
*   **Livrables Attendus** : Ouvrir `tests/core/test_datalink.py`. Ajuster les tests unitaires pour consommer le flux via `next(datalink)` au lieu d'interroger un DataFrame monolithique. Exécuter Pytest. Succès complet.

---

## 📌 Jalon 4 — Reconstruction Neuve de l'Infrastructure Backtrader *Stateless*

Réécriture complète des adaptateurs de l'infrastructure Backtrader pour consommer les nouveaux modèles décimaux et l'évaluation sans état.

### Sous-Jalon 4.1 : Réécriture de l'Outbound Adapter (`BacktraderBrokerAdapter`)
*   **Objectif** : Implémenter l'adaptateur de sortie pour qu'il hérite de `BaseBrokerAdapter`, accepte l'objet immuable `Order` décimal et renvoie un `OrderReceipt`.
*   **Pré-requis** : Jalon 3 validé.
*   **Sous-sous-étapes** :
    1. Réécrire proprement `brokers/backtrader_adapter.py` sous l'interface `BaseBrokerAdapter`.
    2. Implémenter `submit_order(self, order: Order) -> OrderReceipt`. Convertir `order.quantity` (Decimal) en unités d'actifs entières selon les spécifications du registre, et extraire les prix de stops et de limites.
    3. Appeler `self.strategy.buy_bracket()` ou `self.strategy.sell_bracket()` sur Backtrader en fonction d'`order.side`. Renvoyer un `OrderReceipt` typé.
    4. Mettre à jour `get_account_snapshot` pour retourner le modèle exact `AccountSnapshot` décimal.
*   **Livrables Attendus** : Alignement de `tests/brokers/test_backtrader_adapter.py` pour mocker `.submit_order(order)`. Succès total de la suite Pytest sur ce fichier.

### Sous-Jalon 4.2 : Réécriture de l'Inbound Gate et Orchestration Finale
*   **Objectif** : Aligner le pont d'ingestion pour alimenter le `HistoricalBuffer`, interroger le bot via `.evaluate()` et soumettre l'ordre résultant.
*   **Pré-requis** : Sous-Jalon 4.1 validé.
*   **Sous-sous-étapes** :
    1. Réécrire proprement `brokers/backtrader_strategy_bridge.py`. Dans la méthode `__init__`, instancier un `HistoricalBuffer` dimensionné selon la propriété `bot.warm_up_period`.
    2. Dans la méthode `next()`, capturer le prix de clôture de Backtrader, alimenter le buffer glissant via `.append(mid_price)` et construire l'objet immuable `MarketContext`.
    3. Appeler l'évaluation *stateless* : `intent = self.aegis_bot.evaluate(market_context, self.buffer.to_list())`. Connecter l'intention au sizer et au risk manager pour soumettre l'ordre résultant via l'adaptateur outbound.
*   **Livrables Attendus** : Sortir le fichier de test d'intégration `tests/brokers/test_backtrader_bridge.py` de son statut `[DEFERRED]`. Réactiver les scénarios de capture du cycle de vie des ordres et exécuter Pytest. Succès à 100 %.

---

## 📌 Jalon 5 — Sortie d'Hibernation du Test d'Intégration de Bout en Bout (E2E)

Restauration finale de la confiance systémique du framework par la réactivation du grand test de simulation Cerebro.

### Sous-Jalon 5.1 : Réactivation du Test d'Intégration Global
*   **Objectif** : Sortir le fichier `tests/integration/test_backtrader_e2e.py` de son état d'hibernation et valider qu'un bot complet exécute un cycle entier de backtest sans aucune dérive de calcul.
*   **Pré-requis** : Jalon 4 entièrement au vert.
*   **Sous-sous-étapes** :
    1. Ouvrir `tests/integration/test_backtrader_e2e.py` et supprimer le placeholder `[DEFERRED]`.
    2. Adapter la classe locale `IntegrationMeanReversionBot` pour qu'elle hérite de `BaseBot`. Remplacer la méthode à état `_on_bar_close` par la méthode fonctionnelle épurée `evaluate(self, market_context, historical_values) -> ExposureIntent`.
    3. La logique interne doit analyser la liste `historical_values`. Si la longueur correspond aux critères, renvoyer un `ExposureIntent(alpha_direction=1.0)`.
    4. Lancer la commande globale de test de l'écosystème : `./run_tests.sh`.
*   **Livrables Attendus** : Exécution complète de Pytest. La couverture globale de code (Statement Coverage) remonte à 100,00 % sur l'intégralité des packages du framework, et la CI/CD valide le build avec succès.
